import 'package:flutter/material.dart';

class ScannerLog extends StatelessWidget {
  const ScannerLog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(
          child: Card(
            child:Column(children: [
              
            ],)
          ),
        ),
      ),
    );
  }
}
